# FINAL_R1_R8_CLOSURE_AUDIT.md

Audit type: **read / test / analyze only**. No production code modified. Independent temporary probes (no-move FSA mock; real `NoteWriteCoordinator`/`SafeWriter`/`NodeFsVaultStorage` with a delegating `fs/promises` mock; verbatim post-R guard logic with real `MemoryDocumentIndex`/`DefaultDocumentParser`; `MemoryVaultStorage`; real Chromium + real OPFS via the committed e2e) were used and **removed afterward**. Working tree is clean except the pre-existing local `reasonix.toml` change.

## 1. Exact audited SHA

- **Expected remediation SHA:** `89793751af4a5e35202c3a35dc5f3c045e75f4a4` ("fix(concurrency): resolve R1-R8 post-H17 residual data-loss and lifecycle anomalies").
- **Formatting-only follow-up exists:** `576199c65df8b1daf179b443c54f1f3db0055d4b` ("style: format repository files with prettier") — same tree semantics, prettier-only.
- **Current HEAD audited:** `576199c65df8b1daf179b443c54f1f3db0055d4b`. Both commits are on `origin/main`.
- Working tree: clean (only `reasonix.toml` local config, pre-existing).

## 2. Baseline

| Gate                   | Result                                                                                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm ci`               | PASS (0 vulnerabilities)                                                                                                                                                                                                       |
| `npm run format:check` | PASS                                                                                                                                                                                                                           |
| `npm run lint`         | PASS — 0 errors, 4 pre-existing react-hooks warnings                                                                                                                                                                           |
| `npm run typecheck`    | PASS                                                                                                                                                                                                                           |
| `npm test`             | **45 files / 183 tests PASS** (up from 173 — the R1/R2/R3/R4/R5/R6 permanent tests)                                                                                                                                            |
| `npm run build`        | PASS (pre-existing chunk-size warning only)                                                                                                                                                                                    |
| `npm run test:e2e`     | **9/9 PASS** (8 previous + new "Real OPFS + BrowserFSAVaultStorage … in real Chromium (R7)")                                                                                                                                   |
| `npm run verify`       | PASS (format+lint+typecheck+test+build)                                                                                                                                                                                        |
| `npm run verify:full`  | PASS (verify + 9/9 e2e)                                                                                                                                                                                                        |
| Remote GitHub Actions  | **UNAVAILABLE** — repo is private/renamed; `api.github.com` returns 404 for both the repo and actions runs (consistent with every prior audit). CI was replayed locally step-for-step via the `verify:full` script; all green. |

## 3. Verdicts per R1-R8

| #   | Target                                       | Verdict                                                        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | FSA no-move fallback                         | **VERIFIED COMPLETE**                                          | Fallback now fails closed: `browser-fsa-storage.ts` throws `StorageError("Atomic save unsupported …")` instead of the direct in-place write; canonical is never opened for write in the fallback. Independent probe: update → `StorageError`, canonical `EXTERNAL_DATA` intact, **zero `.okw.tmp.` garbage**; create → `StorageError`, no file materialized, no temp garbage; delete-at-entry still yields truthful `ConflictError` and no recreation (permanent tests 6-8). Real Chromium OPFS create/update/no-false-conflict still works (committed e2e test 9, passed).                                                                                                                                                                                                                             |
| R2  | Durable baseline (type during previous save) | **VERIFIED COMPLETE**                                          | `note-coordinator.ts` advances `baselineSnapshot` on **every** successful durable write when not discarded (moved out of the `!stillDirty` gate). Exact mandated interleaving A → B in flight → type C → B durable → C write → discard: **ran 5/5 deterministically, final disk == `B-durable` every run** (independent probe). Permanent Probe J encodes the same interleaving with disk assertions.                                                                                                                                                                                                                                                                                                                                                                                                   |
| R3  | Own delete vs active save                    | **VERIFIED COMPLETE**                                          | Production `deletePath` now: `waitForIdle(path)` → `removeNote(path)` → epoch invalidation → `storage.remove` → `index.remove` → closeTab → refreshVault (useVault.ts). Confirmed **deletePath waits for coordinator idle before disk removal**. Independent probe with the save parked at the post-recheck `fs.rename` gate: `waitForIdle` demonstrably blocks until the parked write commits, then remove lands → `existsAfter=false coordAbsent=true`, **no resurrection**. Permanent Probe K covers the realistic seam interleaving. The documented external-process TOCTOU limit (F-039) is not confused with OpenOb's own operations — own ops are fully serialized.                                                                                                                              |
| R4  | Path reuse after delete/rename               | **VERIFIED COMPLETE**                                          | `indexGenerationMapRef` replaced by `pathEpochMapRef` + `pathSeqMapRef` (+ `vaultRebuildEpochRef`); epochs are bumped/seqs reset on openNote, createNote, createNoteWithProperties, rename (old+new), delete, and vault switch — a new legitimate lifecycle supersedes the old. Independent probes: Case A (delete → create NEW Foo → edit/save) `landed=true`, index/search latest (`EDITED` present, old body absent); Case B (rename → create NEW Foo → edit/save) `landed=true`, index latest. Permanent tests 4/5.                                                                                                                                                                                                                                                                                 |
| R5  | Rebuild vs delayed old upsert                | **VERIFIED COMPLETE**                                          | `refreshVault` bumps `vaultRebuildEpochRef` after `rebuildVaultIndex`; the guard requires `startRebuildEpoch === current` (plus path-epoch + strict seq), so a pre-rebuild delayed upsert is dropped. Independent gated probe: v1 upsert released after a v2 rebuild → `v1Landed=false`, final index == v2, stale-term search = 0 hits. Permanent tests 6 (note) and 7 (`createFolder → refreshVault` variant).                                                                                                                                                                                                                                                                                                                                                                                         |
| R6  | Delay seam                                   | **VERIFIED COMPLETE**                                          | `__setStorageWriteDelay` replaced with single-level indirection: one wrapper installed once (guarded by `__origWrite`), `setDelay` only mutates `delayMs`. Independent measurement: `set(200)`→211 ms; `set(0)`→**0 ms**; `100→200→0`→**0 ms** (no accumulation). Permanent test 8 in index-guard-lifecycle.test.ts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R7  | Permanent coverage truth                     | **VERIFIED COMPLETE** (with explicit mock-vs-real distinction) | Committed permanent tests now include: no-move FSA fallback (browser-fsa-storage tests 6-8 — **MOCK UNIT COVERAGE**, and correctly so: Chromium always has `move()`, so the fail-closed fallback can only be exercised with a mock); exact B→C→discard (Probe J — real coordinator + slow storage); own-delete sequencing (Probe K); path reuse after delete (index-guard 4); path reuse after rename (index-guard 5); rebuild vs delayed upsert (index-guard 6/7); delay-seam reset (index-guard 8); **real Chromium OPFS create/update/no-false-conflict through the real `BrowserFSAVaultStorage`** (e2e test 9 — **REAL BROWSER COVERAGE**, gated on `navigator.storage.getDirectory` support). Mock tests are no longer described as real-browser proof; the OPFS e2e covers the real-handle path. |
| R8  | External TOCTOU documentation                | **VERIFIED COMPLETE**                                          | `FAILURE_REGISTRY.md` F-039 states exactly: internal writes/deletes strictly sequenced via `NoteWriteCoordinator`; external-process recheck→commit window is best-effort; full elimination requires OS-level CAS primitives (`renameat2(RENAME_NOREPLACE)`, file locking) not exposed by Node/browser APIs. In-code comments in both storage adapters reference F-039 with the same boundary. No stronger guarantee is claimed anywhere.                                                                                                                                                                                                                                                                                                                                                                |

## 4. Remaining findings

**P0/P1: NONE.**

**P2/P3 (non-blocking, informational):**

1. **P3 (coverage nuance, not a defect):** the committed R3 test (Probe K) exercises the delay-seam interleaving; the worst-case parked-at-rename interleaving was verified by a temporary probe only (the ordering fix is identical, and `waitForIdle` blocks on `isWriting`/`activePumps` which covers the parked commit). Optional hardening: add the parked-rename variant permanently.
2. **P3 (documentation/known limit):** external-process delete/change in the recheck→rename/move window remains best-effort (F-039). This is a documented inherent limit of standard primitives, not a regression and not reachable by OpenOb's own operations.
3. **P3 (pre-existing):** 4 lint warnings (react-hooks exhaustive-deps) and the 506 kB codemirror chunk warning — pre-existing at every prior audit, non-blocking.

## 5. Regression spot check (item 10)

Targeted re-run, all green (part of the 183): save-promise durability (Probe C, C1, C2 coordinator tests), property + typing (Probe D), AI + typing (Probe E), rename during pending edits (Probe/C2 + e2e H1), FSA save/reload (browser-fsa tests + real-OPFS e2e), Node containment (node-fs-storage-audit traversal + symlink-security suite), secret queue (DesktopSecretStore G6), watcher/verifier ordering (DesktopVaultRuntime C3). Spot-run files: 50 + 21 tests, all passed.

## 6. FINAL DECISION

**UNFREEZE WEB FEATURE DEVELOPMENT — RECOMMENDED.**

All unfreeze criteria are met:

- remote CI fully green — **locally replayed green; remote unverifiable (private repo, 404)** — noted, not a blocker given full local replay of every ci.yml step
- no WEB/SHARED P0/P1 remains — **confirmed** (R1-R3 P1 classes closed with permanent tests)
- R1 safe fallback verified — **yes** (fail-closed, no silent overwrite/resurrection/create-destroy, no temp garbage)
- R2 durable baseline verified — **yes** (5/5 deterministic runs + permanent Probe J)
- R3 own delete cannot resurrect — **yes** (deletePath waits for idle; parked-rename window probe + permanent Probe K)
- real OPFS create/update passes — **yes** (committed e2e, real Chromium)
- existing persistence races remain green — **yes** (183 unit + 9 e2e)

Remaining P2/P3 findings are coverage nuances and the documented external-process TOCTOU limit (F-039) — neither is a reproducible OpenOb data-loss path, so they do **not** continue the freeze.

**Desktop/Electron readiness — reported separately: NOT READY / remains deferred.** Electron was explicitly deferred at the prior gate; the shared storage layer (Node `fs/promises` + coordinator) passes R2/R3 and H10/H11, but Electron readiness was not re-assessed in this closure check and remains out of scope. No claims are made about it here.

This is the closure: the reproducible data-loss classes are closed and the gates are green.
